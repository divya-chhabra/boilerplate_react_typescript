export const config = {
    baseURL: 'https://jsonplaceholder.typicode.com', // Shared base URL
    timeout: 5000,                           // Timeout after 5 seconds
    headers: {
        'Content-Type': 'application/json',     // Global header
    }
}

